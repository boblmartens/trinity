<p>
  <b>Name:</b>
  <%=h @service.name %>
</p>

<p>
  <b>Season of Church Year:</b>
  <%=h @service.season.name %>
</p>

<p><%= link_to "Join the discussion!", service_comments_path(@service.id) %> Currently, there are <%= @service.comments.size %> comments.</p>

<p>
  <b>Description:</b>
  <%=h @service.description %>
</p>

<p>
  <b>Last updated by:</b>
  <%=h @service.user.name %>
</p>

<p><b>Attachments:</b></p>
<ul id="attachment_list">
<% @attachments.each do |attach| %>
  <li>
    <%= link_to attach.name, attached_file_path(attach.id) %> - <%= link_to "[Download]", attach.attached.url %><br />
    Submitted by <%= link_to attach.user.name, user_path(attach.user.id) %> at <%= attach.created_at %>
  </li>
<% end %>
</ul>


<%= link_to 'Edit', edit_service_path(@service) %> |
<%= link_to 'Back', services_path %>
